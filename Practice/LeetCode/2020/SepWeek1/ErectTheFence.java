//package SepWeek1;
import java.util.*;
public class ErectTheFence {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        int trees[][]={{1,1},{2,2},{2,0},{2,4},{3,3},{4,2}};

    }

    public static int[][] outerTrees(int[][] trees) {
        HashMap<Integer,Integer> xl=new HashMap<>();
        HashMap<Integer,Integer> xr=new HashMap<>();
        HashMap<Integer,Integer> yl=new HashMap<>();
        HashMap<Integer,Integer> yh=new HashMap<>();

        for(int i=0;i<trees.length;i++){
            int y=trees[i][1];
            int curr=xl.getOrDefault(x, 0);

        }
    }
}
